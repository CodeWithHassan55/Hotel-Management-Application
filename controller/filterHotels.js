const HotelModel = require("../model/hotelSchema");

const filterHotels = (req, res) => {
    const body = {}
    req.body.map((ele) => {
        switch (ele) {
            case 0:
                price
                break;
            case 1:

                break;
            case 2:

                break;
            case 3:

                break;
            case 4:

                break;
            default:
                break;
        }
    })
}

module.exports = filterHotels;